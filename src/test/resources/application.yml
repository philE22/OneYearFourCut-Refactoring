spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create  # 스키마 자동 생성
    show-sql: true      # SQL 쿼리 출력
    defer-datasource-initialization: true
  profiles:
    include: oauth

#logging:
#  level:
#    org:
#      springframework:
#        transaction:
#          interceptor: TRACE
#      hibernate:
#        type:
#          descriptor:
#            sql: trace